{
  "name": "mongodb-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsc -b && node -r dotenv/config dist/index.js",
    "build": "tsc && node copyPackageJson.js && sam build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^7.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.111",
    "axios": "^1.3.4",
    "dotenv": "^16.0.3"
  }
}
